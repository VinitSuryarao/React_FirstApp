import React from "react"; // React Lib for React elements and Babel Compiler
import ReactDOM from "react-dom"; // React dom for render element.
import HomeComp from "./HomeComp"; // Import Home Component in Index.js

// Render Component on Root Div in Index.html
ReactDOM.render(<HomeComp />, document.getElementById("root"));
